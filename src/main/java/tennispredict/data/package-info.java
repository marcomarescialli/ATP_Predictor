/**
 * Data layer (lowest). Parses the raw ATP CSVs into clean, immutable Java
 * objects: {@code Surface}, {@code Player}, {@code RawMatch}, {@code CsvParser}.
 *
 * <p>Dependency rule: this package depends on nothing else in the project.
 * Everything else may depend on it; it depends on no one. Phase 1 fills it in.
 */
package tennispredict.data;
